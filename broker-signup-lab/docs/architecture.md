# Architecture

Static GitHub Pages application.

- `analysis.js`: seeded synthetic cohort + pure analysis functions
- `app.js`: UI state, filtering, rendering
- `index.html`: reviewer narrative and semantic structure
- `styles.css`: presentation layer
- `test.mjs`: golden-case verification

No backend, third-party analytics, customer data, external runtime dependency, or hidden model call is required.
