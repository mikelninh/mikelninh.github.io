# Verification

Run:

```bash
node broker-signup-lab/test.mjs
```

The gate verifies cohort size, golden funnel counts, monotonic funnel behaviour, largest stage drop, largest recoverable issue, hotspot segment, bounded opportunity model, and a stronger mobile timeout signal.
